DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
# Used by the Prisma CLI (prisma.config.ts) and by the manual ingestion
# script (scripts/ingest.ts, run via `npm run ingest`) — see
# src/modules/ingestion/llmSummarizer.ts.
GEMINI_API_KEY=""
GROQ_API_KEY=""
